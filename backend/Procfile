web: python -c "from app.db.base import Base; from app.db.session import engine; from app.models import *; Base.metadata.create_all(bind=engine); print('Tables OK')" && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}
